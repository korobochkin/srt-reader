---
services:
  tests-runner:
    build:
      dockerfile: ./development/php/Dockerfile
      context: ./../
      target: base
    tty: true
    volumes:
      - ./../:/root/app
      - ./../tmp/composer:/root/.composer
